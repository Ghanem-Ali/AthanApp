<Window x:Class="AdhanApp.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="الإعدادات" SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Deactivated="Window_Deactivated">

    <Window.Resources>
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Padding="12,10"
            BorderThickness="1" BorderBrush="#444444">
        <Border.Background>
            <SolidColorBrush Color="#1A1A1A" Opacity="0.9"/>
        </Border.Background>

        <StackPanel FlowDirection="RightToLeft">
            <TextBlock Text="قم بوضع احداثياتك" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,10" 
                       Style="{StaticResource LabelStyle}"
                       FontSize="13" 
                       Opacity="0.8"
                       FontWeight="Bold"/>

            <!-- Latitude -->
            <StackPanel Orientation="Horizontal" Margin="0,2">
                <TextBlock Text="خط العرض" Style="{StaticResource LabelStyle}" Margin="0,0,0,0"/>
                <TextBox x:Name="txtLat" Style="{StaticResource InputStyle}"
                         FlowDirection="LeftToRight" Margin="8,0,0,0"
                         LostFocus="OnValueChanged" KeyDown="OnKeyDown"/>
            </StackPanel>

            <!-- Longitude -->
            <StackPanel Orientation="Horizontal" Margin="0,2">
                <TextBlock Text="خط الطول" Style="{StaticResource LabelStyle}" Margin="0,0,0,0"/>
                <TextBox x:Name="txtLng" Style="{StaticResource InputStyle}"
                         FlowDirection="LeftToRight" Margin="10,0,0,0"
                         LostFocus="OnValueChanged" KeyDown="OnKeyDown"/>
            </StackPanel>

            <Separator Background="#444444" Margin="0,10,0,10" Opacity="0.5"/>

            <!-- Screen Selection -->
            <StackPanel Orientation="Horizontal" Margin="0,2">
                <TextBlock Text="الشاشة" Style="{StaticResource LabelStyle}"/>
                <ComboBox x:Name="comboScreen" Margin="30,0,0,0" Width="100" Height="25"
                          Foreground="White" BorderBrush="#444" BorderThickness="1" 
                          SelectionChanged="OnValueChanged">
                    <ComboBox.Template>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton" Background="#333" BorderBrush="{TemplateBinding BorderBrush}"
                                              Foreground="White" Grid.Column="2" Focusable="false"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="Border" CornerRadius="4" Background="#333" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                                                <Path x:Name="Arrow" Fill="White" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0" Data="M0,0 L4,4 L8,0 Z"/>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  Margin="5,0,20,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Border x:Name="DropDownBorder" Background="#333" BorderThickness="1" BorderBrush="#444" CornerRadius="2">
                                        <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </ComboBox.Template>
                    <ComboBox.Resources>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Background" Value="#333"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Padding" Value="5,2"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#555"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.Resources>
                </ComboBox>
            </StackPanel>

            <!-- Position Selection (Visual) -->
            <StackPanel Orientation="Horizontal" Margin="0,10,0,2">
                <TextBlock Text="موقع النافذة" Style="{StaticResource LabelStyle}" VerticalAlignment="Top" Margin="0,5,0,0"/>
                <Border Width="100" Height="70" Background="#252525" CornerRadius="4" 
                        BorderBrush="#444" BorderThickness="1" Margin="15,0,0,0">
                    <Grid x:Name="gridPositionSelector" Margin="2" FlowDirection="LeftToRight">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Rectangle x:Name="posTopLeft" Grid.Row="0" Grid.Column="0" Width="14" Height="10" Fill="#555" RadiusX="2" RadiusY="2" Cursor="Hand" MouseLeftButtonDown="Pos_Click" Tag="TopLeft"/>
                        <Rectangle x:Name="posTopRight" Grid.Row="0" Grid.Column="2" Width="14" Height="10" Fill="#555" RadiusX="2" RadiusY="2" Cursor="Hand" MouseLeftButtonDown="Pos_Click" Tag="TopRight"/>
                        <Rectangle x:Name="posCenter" Grid.Row="1" Grid.Column="1" Width="14" Height="10" Fill="#555" RadiusX="2" RadiusY="2" Cursor="Hand" MouseLeftButtonDown="Pos_Click" Tag="Center"/>
                        <Rectangle x:Name="posBottomLeft" Grid.Row="2" Grid.Column="0" Width="14" Height="10" Fill="#555" RadiusX="2" RadiusY="2" Cursor="Hand" MouseLeftButtonDown="Pos_Click" Tag="BottomLeft"/>
                        <Rectangle x:Name="posBottomRight" Grid.Row="2" Grid.Column="2" Width="14" Height="10" Fill="#555" RadiusX="2" RadiusY="2" Cursor="Hand" MouseLeftButtonDown="Pos_Click" Tag="BottomRight"/>
                    </Grid>
                </Border>
            </StackPanel>

            <Separator Background="#444444" Margin="0,10,0,10" Opacity="0.5"/>

            <!-- Notifications Toggle -->
            <StackPanel Orientation="Horizontal" Margin="0,2">
                <TextBlock Text="الإشعارات" Style="{StaticResource LabelStyle}"/>
                <ToggleButton x:Name="toggleNotifications"
                              VerticalAlignment="Center"
                              Margin="12,0,0,0" Width="38" Height="20"
                              Checked="OnToggleChanged" Unchecked="OnToggleChanged">
                    <ToggleButton.Style>
                        <Style TargetType="ToggleButton">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="track" Width="38" Height="20"
                                                CornerRadius="10" Background="#555" Cursor="Hand">
                                            <Ellipse x:Name="thumb" Width="14" Height="14"
                                                     Fill="White" HorizontalAlignment="Left"
                                                     Margin="3,0,0,0">
                                                <Ellipse.RenderTransform>
                                                    <TranslateTransform x:Name="thumbTranslate" X="0"/>
                                                </Ellipse.RenderTransform>
                                            </Ellipse>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="track" Property="Background" Value="#4CAF50"/>
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetName="thumbTranslate"
                                                                             Storyboard.TargetProperty="X"
                                                                             To="18" Duration="0:0:0.15"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetName="thumbTranslate"
                                                                             Storyboard.TargetProperty="X"
                                                                             To="0" Duration="0:0:0.15"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ToggleButton.Style>
                </ToggleButton>
            </StackPanel>

        </StackPanel>
    </Border>
</Window>
